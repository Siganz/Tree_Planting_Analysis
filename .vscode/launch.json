{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Download Data",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/bin/download_data",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/src"
      }
    }
  ]
}
